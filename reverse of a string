#include <stdio.h>
int main()
{
    char str1[80];
    char str2[80];
    printf("enter string\n");
   scanf("%[^\n]s",str1);
    printf("reverse of string is:\n");
    int c=strlen(str1);
    for(int i=0;i<c;i++)
    str2[i]=str1[c-i-1];
    puts(str2);
    return 0;
}
