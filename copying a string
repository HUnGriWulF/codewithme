#include <stdio.h>
#include <string.h>
int main ()
{
    char str1[80],str2[80];
    printf("input string 1\n");
    gets(str1);
    int l=strlen(str1);
    for(int i=0;i<l;i++)
    {
        str2[i]=str1[i];
    }
    printf("first string: %s second string is %s",str1,str2);
    return 0;
}
